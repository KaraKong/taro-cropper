<block wx:if="{{$taroCompReady}}">
    <view style="{{anonymousState__temp}}">
        <view class="ss" style="{{anonymousState__temp4}}">{{temp}}</view>
        <block wx:if="{{imgSrc}}">
            <cropper id="hzzzz" compid="{{$compid__7}}"></cropper>
        </block>
        <button bindtap="onUploadPhoto">上传图片</button>
        <button bindtap="onCropImg">确定</button>
        <block wx:if="{{cropImg}}">
            <view>
                <image style="{{anonymousState__temp3}}" src="{{cropImg}}" mode="widthFix"></image>
            </view>
        </block>
    </view>
</block>